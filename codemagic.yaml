workflows:
  default:
    name: BossCash
    max_build_duration: 10
    instance_type: mac_mini_m1
    scripts:
      - name: Forzar compilación de Android
        script: |
          flutter create . --platforms=android
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
