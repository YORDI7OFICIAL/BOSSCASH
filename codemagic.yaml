workflows:
  android-release:
    name: Android Release APK
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      flutter: stable
    triggering:
      events:
        - push
    scripts:
      - name: Limpiar y reparar Android v1 embedding
        script: |
          flutter clean
          flutter create . --platforms=android
      - name: Compilar APK modo Release
        script: |
